import { ToasterProps } from "react-hot-toast";

const configToast: ToasterProps = {
  position: "top-right",
  gutter: 8,
  reverseOrder: false,
  toastOptions: {
    duration: 5000,
  },
};

export default configToast;
